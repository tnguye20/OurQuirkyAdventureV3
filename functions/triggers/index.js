const { extractImageMeta } = require('./extractImageMeta');
const { removeMemory } = require('./removeMemory');
const { removeMemoryFile } = require('./removeMemoryFile');

module.exports = {
  extractImageMeta,
  removeMemory,
  removeMemoryFile
}
