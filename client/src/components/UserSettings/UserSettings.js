import React, { useState } from 'react';
import {
  Container,
} from '@material-ui/core';

export const UserSettings = () => {
  return (
    <Container maxWidth="md">
      <h1>User Settings</h1>
    </Container>
  )
}
